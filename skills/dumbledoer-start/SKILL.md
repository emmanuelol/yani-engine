---
name: dumbledoer-start
description: Registers an atomic task plan and initiates the DumbleDoer orchestrator for the current project.
---

# DumbleDoer Start

When the user invokes `/dumbledoer start` or asks to start a DumbleDoer session, execute the globally installed command:

```bash
dumbledoer start --docs ./docs
```

This ensures the native DumbleDoer CLI manages the orchestration.
