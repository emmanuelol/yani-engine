{
  "name": "start",
  "description": "Ingests documentation, conducts a discovery phase, and maps out an atomic task plan in memory.md.",
  "execute": {
    "command": "./run_dumbledoer.sh",
    "args": ["start"]
  }
}